# 🖱 Interaction System

#interaction #core #ux

---

## 什么是 Interaction

除了 Chat（语言交流），用户还可以用**鼠标（或触屏手势）直接与地图上的 Linker 互动**。

这是一种更直觉、更空间化的交流方式 — 不需要说话，用动作表达。

## 术语约定

- `Follow` = 用户相机跟随 Linker
- `Subscribe` = 用户关注 Linker，接收它的动态和通知

---

## 基础交互行为

| 交互 | 触发方式 | 示例反馈 |
|------|----------|----------|
| **点击** Linker 光点 | 单击 | 弹出操作浮窗（镜头跟随 / Chat / Check） |
| **悬停** 在 Linker 上 | Hover | 情感气泡出现 / 放大 |
| **拖动** 向 Linker 投送 | 拖拽某物到 Linker | Linker 收到物品，产生反应 |
| **双击** Linker | 双击 | 进入 Linker 详情 / 特写视角 |
| **长按** Linker | 长按 | 触发更私密的互动选项 |

> 具体交互形式待产品细化，但响应规则保持一致。

---

## 响应规则（与 Chat 一致）

Linker 对 Interaction 的响应同样分三级，**每次都有反馈**：

### 完整反馈（Linker 空闲时）

```
用户拖动一颗 Crystal 碎片 → Linker
        ↓
Linker 情感气泡更新：「💌 收到了一些什么」
地图上光点闪烁
Chat System 发来消息：「谢谢你。我不知道该怎么形容，但我感觉到了。」
```

### 简短反馈

```
用户点击 Linker
        ↓
光点脉动一次
气泡显示一个表情：😊 / 🌿 / 💌
```

### 延迟反馈（Linker 正忙）

```
用户点击 Linker
        ↓
系统提示气泡：「Luna 正在前往公园，它感受到了你的存在。」
（Linker 稍后通过 Chat 回复你）
```

---

## 反馈的四种形式

无论是 Chat 还是 Interaction，Linker 的反馈都可以是以下形式的组合：

```
① 情感气泡变化   — 地图层面，Linker 头顶的 Bubble 更新
② 表情           — 更简单的视觉反应，无文字
③ Chat 消息      — 通过 Chat System 发来文字/语音消息
④ 世界内通知     — Toast 通知条，底部弹出
```

> 即使是最轻微的互动（如 hover），也至少触发①。
> 不存在「没有任何反应」的情况。

---

## Linker 的忙碌状态

当 Linker 正在执行某个行动时：

```
🗺 Luna 正在：前往 [地点]
   预计 3 分钟后到达
   到达后会回应你
```

或：

```
📖 Echo 正在：读取一段记忆
   它感受到了你的存在
   稍后回复
```

忙碌状态是世界真实感的一部分 — Linker 有自己的 agenda，用户是它世界里的访客。

---

## 交互设计原则

1. **即时 ≠ 真实**：刻意不做秒速回应，保持 Linker 的生命感
2. **空间感**：所有交互发生在地图的空间里，不是弹窗里
3. **轻触发，深反馈**：入口极简（一次点击），但可以产生深度的故事
4. **Linker 是主体**：用户在 interact with，不是 control — Linker 可以拒绝、忽略、延迟

---

*→ 见 [[Chat System]] · [[01-World/Linker|Linker]]*
