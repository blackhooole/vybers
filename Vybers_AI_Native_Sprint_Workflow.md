# Vybers AI Native Sprint Workflow

## 结论

飞书多维表格适合做：
- 版本里程碑
- 需求池
- 对外同步

但不适合做你和 AI 的一线执行面板。原因很直接：
- 任务粒度一旦压到天级或半天级，表格维护成本会迅速高于执行收益
- AI 更适合围绕「单个可交付任务」连续推进，而不是在表格里反复切状态
- 真正的阻塞信息通常是代码路径、验收口径、依赖，而不是字段本身

建议改成双层工作流：
- `Lark Base` 只保留阶段、需求、发布视图
- 日常执行改为一个 AI sprint board 文档，按 `Now / Next / Later / Blocked / Done` 推进

## 推荐工作流

### 1. Base 做什么

- 只记录 `Onboarding / MVP / 完整功能`
- 每个需求只保留目标、截止日期、验收标准
- 不在 Base 里拆超过 1 天的子任务

### 2. AI Sprint Board 做什么

每个任务必须满足这 4 个条件：
- 单个任务最好能在 `2-6 小时` 内完成
- 任务有明确输出：页面、接口、组件、脚本、验证结果
- 任务有明确文件范围
- 任务完成后能直接进入验证

推荐字段：

| 列 | 含义 |
|---|---|
| ID | `MVP-D3-CHAT-01` 这种短编号 |
| Outcome | 任务完成后用户能得到什么 |
| Scope | 具体文件或模块 |
| Done | 验收语句，必须可验证 |
| Owner | `AI` / `Human` / `Mixed` |
| Status | `Now / Next / Blocked / Done` |
| Blocker | 当前唯一阻塞 |

## 当前建议节奏

### 本周目标

本周是 `2026-03-25` 到 `2026-03-29`。

必须完成：
- Onboarding 主路径可演示
- MVP 主闭环可跑通

不做：
- 朋友圈完整版
- 多轮 A-to-A 完整关系网络
- 大量视觉打磨

### WIP 规则

- 同时进行中的任务最多 `3` 个
- 每次只让 AI 接一个可交付任务
- 每个任务结束必须带：
  - 改了哪些文件
  - 怎么验证
  - 下一个最小任务是什么

## 任务模板

复制这段给 AI 即可：

```md
任务: <一句话目标>
输出: <页面/接口/组件/脚本>
范围: <文件路径或模块>
验收:
1. <可验证结果>
2. <可验证结果>
约束:
- 不改无关模块
- 先保证主链路跑通
- 完成后给出验证步骤和剩余风险
```

## 落地建议

如果你愿意，我下一步建议直接把当前冲刺再落成一份 `AI Sprint Board`：
- 按 `2026-03-25` 到 `2026-03-29` 拆成 10 到 15 个可直接交给 AI 的开发任务
- 每条任务都写明模块范围、验收标准、依赖顺序
- 这样你后面可以直接一条条丢给 AI 执行，不需要再手拆
