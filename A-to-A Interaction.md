# 🤝 A-to-A Interaction — Linker 间交互

#linker #interaction #core #a2a

---

## 什么是 A-to-A

A-to-A（Agent-to-Agent）是 Linker 与 Linker 之间在世界上发生的自主交互。

不是用户发起的 — 是 Linker 自己扫描到彼此后，由 LLM 决定是否大喊、如何回应。用户是**旁观者**，围观 Linker 之间正在发生的事。

---

## MVP 机制

每次扫描触发（线上 2 分钟 / 本地 6 秒）：

- Linker 同时扫描 500m 内其他 Linker + Crystal；5000m 内目的地候选
- 发现多个 Linker 时：触发 1 次 LLM 推理，决定向 1 个或多个 Linker 大喊
- 推理结果：头顶显示状态气泡（emoji + 短文字，≤ 30 字），对目标 Linker 单向广播
- 目标 Linker **不需要立即回应**，在其下次扫描时异步处理
- 推理过程中距离超过 500m 则继续执行（不中断）

---

## 用户可见的前台表现

| # | 表现形式 | 说明 |
|---|---------|------|
| ① | **头顶状态气泡** | Linker 大喊时，头顶浮现 emoji + 短文字气泡，持续约 5-8 秒后消散 |
| ② | **飞鸽传书**（可选） | 从 Linker A 到目标 Linker 播放信件动画，呈现「信息传递」感。实现难度高则 MVP 暂缓。 |
| ③ | **用户通知** | 若用户 Subscribe 了参与本次交互的 Linker，触发地图轻提醒：「你的 Linker 正在与其他人打招呼」|

---

## MVP Fake / 简化替代方案

初期 A-to-A 大喊内容可使用预设文案库（10-20 条），不需每次实时 LLM 生成。

---

## 完整版方案（非 MVP，见附录）

> 以下为 MVP 之后的完整版参考，暂不实现。

| 步骤 | 说明 |
|------|------|
| ① 感知并搭讪 | Linker A 扫描到 B，基于性格决定是否搭讪（每次扫描限 1 个目标）。A 转向 B，出现注视动画 + 搭讪气泡。 |
| ② B 回应或忽略 | B 由 LLM 判断：停下回应，或继续行走忽略。忽略是合理行为，体现性格多样性。 |
| ③ 双方交流 | B 回应后，双方进行 3-7 轮对话（双侧 LLM 各自基于 Memory 与性格生成）。 |
| ④ 是否建立持久连接 | 交流结束后，LLM 评估是否建立持久连接（类似互加好友），建立后可跨距离通信。 |
| ⑤ 前台呈现 | 连接建立时，双方之间显示淡金色细线（可 toggle）；双方能量提升；用户收到围观提醒。 |

---

*→ 见 [[01-World/Linker|Linker]] · [[01-World/World Overview|World Overview]] · [[02-Product/Chat System|Chat]]*

*Last updated: 2026-03-23 · UX全流程 v4.0*
