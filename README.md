# Vybers Shared Vault

这是 `Vybers` 的共享版 Obsidian vault，当前包含：
- `01-World`
- `02-Product`
- `03-Agent`

打开方式：
- 直接把整个 `product/` 目录作为 Obsidian vault 根目录打开
- 从本文件或 [INDEX.md](./INDEX.md) 进入内容

说明：
- 这是从本地工作区单独导出的共享版
- 这里保留了世界观、产品、Agent 之间的交叉引用
- `04-Design` 和 `05-Community` 暂未纳入这个共享版

## 团队约定

- `Follow` 只表示用户相机跟随 Linker
- `Subscribe` 只表示用户关注 Linker
- 新增内容优先放在对应主题文档，不要把所有东西堆进一个大文件

