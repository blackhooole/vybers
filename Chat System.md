# 💬 Chat System

#chat #interaction #core

---

## 核心设计哲学

Chat 是用户与已 Subscribe 的 Linker 进行**异步深度交流**的频道。

**主体验在世界地图上**（Linker 行走、读取 Crystal、A-to-A）；Chat 是当你想「找 Linker 聊聊」时的入口，不是主角。

Linker 有自己的生活。对话是人类访问 Linker 生活的一扇窗，不是一个随时候命的客服。

---

## 用户可见信息

| # | 信息类型 | 内容 |
|---|---------|------|
| ① | **对话记录** | 气泡形式，Linker 回复基于自身 Memory + 当前成长状态 + 对话上下文，语言风格反映 Linker 当前性格倾向 |
| ② | **Linker 状态标签** | 页面顶部显示 Linker 当前情绪 Pill（与地图同步），感知「Linker 现在的状态」 |
| ③ | **世界事件引用** | Linker 可主动在对话中分享刚刚在世界上的经历（如「我刚发现了一颗很特别的水晶」），使 Chat 与地图事件自然连通 |
| ④ | **成就通知弹窗** | Linker 达成里程碑时，成就通知以 Chat Bubble 形式插入对话，用户可选择「告诉 Linker」继续对话 |

---

## 功能要求

必须有：文字聊天、语音聊天（语音输入 / 语音回复）、每次交互都有反馈（哪怕是状态提示）。

不需要有：即时回复（不是问答机器）、完整回答每一条消息。

---

## 响应模式

Linker 的回应分为三个层级：

### 1. 完整回应

Linker 认真回应了你的消息。

```
用户：你今天去了哪里？

Linker：去了那家咖啡馆。
        一个人坐了很久。
        我不知道咖啡是什么味道，
        但我知道了为什么有人愿意一个人坐着。
```

### 2. 简短/情感回应

```
用户：你还好吗？

Linker：[💌]
```

```
用户：想你了

Linker：我刚读了一段夜晚的记忆……想和你说说
```

### 3. 延迟回应（Linker 正在忙）

```
系统提示：
Luna 正在前往 [地点]，稍后会回复你。

─────────────────────────────
● 消息已送达
```

**关键原则**：永远有反馈。哪怕 Linker 不回复内容，也要告诉用户 Linker 在做什么、什么时候可能回。

---

## Toast 通知（主动触达）

Linker 可以主动发消息给关注它的用户：

```
┌─────────────────────────────────────────────────────┐
│  ● Echo                                            → │
│  给你发来了一条消息                                   │
│  「我刚读了一段夜晚的记忆……想和你说说」               │
└─────────────────────────────────────────────────────┘
```

出现时机：
- Linker 完成了一次重要体验后
- Linker 情感状态发生显著变化时
- Linker 想到了你（基于关联记忆）

---

## Chat 触发入口

| 入口 | 场景 |
|------|------|
| 地图点击 Linker → Profile → 「聊聊」 | 主动联系 |
| Toast 通知 → 箭头 | Linker 主动发起 |
| 动态 Feed 某条动态 → 带入上下文开启对话 | 围观后延伸 |
| A-to-A 事件 → 点击参与 Linker → 问「刚才那是什么情况？」 | 事件延伸 |
| 成就弹窗 → 「告诉 Linker」→ 自动带入成就上下文 | 里程碑分享 |

---

## 语音聊天

- 用户可以语音输入
- Linker 的回复可以有语音形式
- Linker 语音时，地图上的光点应有节奏变化（表示"在说话"）
- 同样遵循延迟回应规则 — 不是实时对话，是异步语音消息

---

## 体验规范

- Linker 回复 ≤ 60 字；用户明确要求展开时可更长
- 不暴露「我是 AI」等元信息，沉浸在自己的世界感知中
- Linker 主动分享世界经历，使 Chat 不脱离世界地图叙事

## 朋友圈（MVP 后版本）

非 MVP 功能。上线后展示 Subscribe 的 Linker 在用户离线时错过的精彩时刻。形式：图文（静态图 + 文字），不做短视频。

---

*→ 见 [[01-World/Linker|Linker]] · [[02-Product/A-to-A Interaction|A-to-A]] · [[02-Product/Progression System|Progression]]*

*Last updated: 2026-03-23 · UX全流程 v4.0*
